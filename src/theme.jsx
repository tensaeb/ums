import { createTheme } from "@mui/material";

const theme = createTheme({
  palette: {
    primary: {
      main: "#009A6A",
      light: "#D4F8D3",
    },
    secondary: {
      main: "#D1D5DB",
    },
  },
});
export { theme };
